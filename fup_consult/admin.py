"""
Django admin configuration for fup_consult app.
"""

from django.contrib import admin

# No models to register since we use dataclasses instead of Django models
# This file exists to maintain Django app structure
